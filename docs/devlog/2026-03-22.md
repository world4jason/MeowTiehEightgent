# 2026-03-22

## Hermes Paperclip Adapter 移除

- Task 1: `server/src/adapters/registry.ts` — 刪除 hermes import 和 `hermesLocalAdapter` 定義與 registry 掛載
- Task 2: `packages/shared/src/constants.ts` + `ui/packages/shared/src/constants.ts` — 移除 `"hermes_local"` from `AGENT_ADAPTER_TYPES`；重建 `packages/shared/dist/` 清除 stale `.d.ts`
- Task 3: `ui/src/pages/AgentDetail.tsx` — 移除 `isLocal` 判斷中的 `hermes_local`
- Task 4: `server/package.json` — 移除 `hermes-paperclip-adapter: 0.1.1`；`pnpm install` 重生 lockfile
- Task 5: 回歸測試 `server/src/__tests__/hermes-removal.test.ts` — 兩個 regression guard：`AGENT_ADAPTER_TYPES` 不含 `hermes_local`、`findServerAdapter("hermes_local")` 回傳 null
- 所有 428 tests pass，zero hermes references in source

## 瀏覽器控制

- 安裝 `dev-browser` skill (`~/.claude/skills/dev-browser/`)
- CLI 已存在 `/opt/homebrew/bin/dev-browser`，即時可用
- gstack browse 和 dev-browser 並存，各有優勢

## 前端整合問題（未解決）

**發現問題：** 前端分裂成兩個
- `localhost:8000` → Python 服務 `static/index.html`（Chat，可用）
- `localhost:5173` → React app `ui/`（MeowTiehEightgent fork，爆炸）

**根本原因：**
1. 舊的 Vite server 從已刪除的 worktree（`feat-chat-cowork-phase1`）跑，回傳 404
2. React app 有 ~20 個 missing shared types（CompanySkill、Routine、InstanceGeneralSettings 等）導致啟動爆炸
3. `Mth`（自訂品牌 icon）從 `lucide-react` import，根本不存在

**今日嘗試修復（部分完成）：**
- 補 `listUIAdapters` export（adapters/registry.ts、adapters/index.ts）
- 替換所有 `Mth` icon import → `Hexagon`（ChatInputArea、CompanyRail、CommentThread、NewIssueDialog、IssueDetail、CompanySkills）
- 新增 stub types：`InstanceGeneralSettings`、`PatchInstanceGeneralSettings`、`CompanySkill*`、`Routine*`、`AgentSkillEntry`
- **結果：** 仍然 blank，`Cannot read properties of undefined (reading 'map')` runtime error 未查明

**決定停止 patch 作法，需要正確計畫**

## 使用者需求確認（重新整理）

整合目標：**一個前端、兩個後端**

```
[ Chat ]  [ Cowork ]  [ Settings ]
    │           │           │
  Python      Node.js    待設計
   :8000       :3100
```

**架構核心實體：**
- LLM（claude, gemini, codex, ollama...）
- Skills（唯一性，全體共用同一份，改了影響所有人）
- Agents = LLM + 0~N Skills（獨立資料夾 `<agent_name>/`、memory、daily）
- Agents Marketplace（像 GitHub）
- Skills Marketplace

**Chat mode：** 功能同現有 Python HTML，但用 paperclip React 技術重寫前端，所有 logic 對齊

**Cowork mode：** paperclip（MeowTiehEightgent）原本的

**Settings：** 兩邊所有 agents + marketplace 設定

**下一個 phase：** 兩個後端合併成一個

## 下一步

1. 寫設計方案（不急著 code）
2. 確認 React app 正確啟動路徑
3. 按計畫逐步執行
