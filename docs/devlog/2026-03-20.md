# 2026-03-20

## Skill Source / Namespace System

- 安裝 gstack (`skills/gstack/`) — 21 skills 透過 symlinks 展開至 `skills/`
- Backend: `parse_skill()` 偵測 gstack symlink，自動補 `source: "gstack"`, `source_url`, `source_version`
- Frontmatter 支援手動宣告 `source:` 供任何 skill 使用（未來擴充）
- `/skills` API 回傳 `display_name = "gstack:review"` 格式，附帶 `source_url` 和版本資訊
- `resolve_human_text()` 支援 `/gstack:review` 格式呼叫，fallback 到 slug 查找
- UI: skill chips 查表顯示帶來源的名稱（`gstack:review` 而非 `review`）
