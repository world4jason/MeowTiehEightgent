# Devlog 2026-03-23

## Settings Page Rebuild (Complete)

Rebuilt the entire React Settings page from a 71-line placeholder to a full 8-tab panel:

- **SettingsShell** — tab nav framework (模型/代理人/代理人市場/技能/情境模板/工作區/靈魂/關於)
- **ModelsTab** — CRUD for CLI + Ollama API models, including model pull
- **AgentsTab** — CRUD + AGENT.md/IDENTITY.md/SOUL.md editing + skills assignment + test connection + **Cowork agents (read-only with badge)**
- **AgentMarketTab** — marketplace browse + fork install + manual agent creation + **full template CRUD (上架/編輯/刪除)**
- **ScenariosTab** — **NEW** scenario template CRUD (name, description, system_prompt, topic_hint, suggested_agents)
- **SkillsTab** — skill CRUD + .zip upload
- **WorkspacesTab** — workspace CRUD + file management + default agents
- **SoulTab** — 4-card default template editor
- **AboutTab** — static system info

Total: 10 files, ~3500 lines. All backed by `useSettingsApi.ts` (500+ lines, 40+ react-query hooks).

Spec: `docs/superpowers/specs/2026-03-23-react-settings-redesign.md`
Plan: `docs/superpowers/plans/2026-03-23-react-settings-rebuild.md`

## Marketplace CRUD

Added full CRUD for Agent Marketplace templates:
- **Backend**: POST/PUT/DELETE `/marketplace/agents` endpoints
- **Frontend**: 「+ 上架模板」form with MD editors, 「編輯」/「刪除」in detail view
- 3 new react-query hooks

## Scenario Template CRUD

Added full CRUD for Scenario templates:
- **Backend**: GET/POST/PUT/DELETE `/scenarios` endpoints
- **Frontend**: New 「情境模板」Settings tab with list+detail, system prompt, topic hint, agent checkboxes
- 5 new react-query hooks

## Chat Features — TDD (All P0 + P1 + P2 Complete)

### P0 — Core

- [x] **Markdown rendering** — MarkdownBody for agent messages (GFM, code blocks, links, mermaid). 8 tests.
- [x] **Thinking animation** — WS `thinking` message → animated "思考中 ●●●" dots. 4 tests.
- [x] **Stop button** — Red stop button sends WS `stop` when streaming/thinking.
- [x] **Auto/Manual mode toggle** — Auto (green) / Manual (amber) in WelcomeScreen + ChatInputArea.
- [x] **Next → button** — Handle WS `ready` with `pause:true`, green "Next →" button sends `next`.

### P1 — Important

- [x] **Welcome Screen 完整化** — Workspace/Scenario/Blank context tabs, topic input, "開始對話" button, MeowTiehEightgent branding.
- [x] **Scenario + Topic 共存** — Scenario card toggle-selects (doesn't immediately start). Topic input shows scenario's `topicHint` as placeholder. Both sent together on start.
- [x] **Rounds 設定** — Number input (1-20) in Manual mode.
- [x] **機率性沈默** — Silence checkbox.
- [x] **Image Lightbox** — Full-screen overlay, Escape/click-outside to close.
- [x] **Message timestamps** — HH:mm next to agent names.
- [x] **Topic in header** — Extract from system message, display in ChatHeader.

### P2 — Experience

- [x] **Enter-to-Send toggle** — ↵ button, localStorage persistence.
- [x] **Message queue** — Buffer messages while streaming, "佇列中: N" badge, auto-drain.
- [x] **Text file attachments** — .txt/.md/.json/.csv/.py/.js/.ts as code blocks.
- [x] **Context-aware placeholder** — Different hints for paused/streaming/manual states.

## Bug Fixes

- **Cowork health check**: Vite-proxied `/api/health` to avoid CORS
- **Cowork onboarding overlay**: `z-[60]` header stays above z-50 wizard
- **Session sort order**: `x.stat().st_mtime` instead of `x.name`
- **New Chat duplicate**: `isPending` guard
- **WS 403**: Caused by zombie connections hitting 3-per-IP limit; server restart clears
- **test-setup.ts**: Guard `Element.prototype.scrollIntoView` for node env tests

## Eng Review Fixes

- **C1**: AgentsTab `handleSave` try/catch + error banner (auto-clear 5s)
- **C2**: `encodeURIComponent(filename)` in workspace file deletion
- **C3**: EditTemplateForm setState-during-render → useEffect
- **I1**: Cowork agents visible in AgentsTab (read-only, badge)
- **I2**: `isValidName` extracted to shared `settings/utils.ts`
- **I4**: Workspace "已儲存" badge auto-clears after 3s
- **M2**: Marketplace install form name validation

## Test Summary

236 tests across 30 files, all passing. ~60 new TDD tests added today.

## CLAUDE.md

Created project-root `CLAUDE.md` with architecture, data model, settings design principles, and key file locations.
