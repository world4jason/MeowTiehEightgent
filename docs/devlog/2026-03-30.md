# Devlog 2026-03-30 — Bug Fix Sprint + Architecture Planning

## Summary

12 commits。主要是 v0.11.0 後的 bug fix sprint 和兩個新功能。

## Bug Fixes

### Model 連線全修通
- Adapter preset suffix fallback（`gemini_local` → `gemini`）
- Codex CLI args 更新（`-q -p` → `exec`）
- Ollama API error surfacing
- Gemini `-e ""` 跳過 extension loading（56s → 27s）
- 測試連線搬到模型 tab，CLI 用快 model 測，API 用用戶設定

### B5 — Skill Prompt Bloat（最大改善）
- build_prompt 從注入 skill 全文改成只注入名稱+描述
- **24,509 → 4,409 chars（-82%）**
- Agent 回應速度應明顯改善

### B6 — Welcome Screen Textbox 殘留
- showWelcome() 清空 topic input

### Token 追蹤修復
- `_get_json_output_flags` 改用 `cmd[0]` binary name 判斷
- 修復 v1 agent（`claude_local`）沒有 stream-json flag 的問題

## 新功能

### Prompt Breakdown（字元組成分析）
- `build_prompt()` 回傳 `(str, dict)` tuple
- Breakdown dict: identity / history / skills / memory / other / total
- `message_end` WS event 附上 `prompt_breakdown`
- 前端 bar chart 尚未做

### Skill Validated Tag
- `parse_skill()` 解析 `validated: true/false` from frontmatter
- Prompt 顯示 ✅ 已驗證 / ⚠️ 未驗證
- Boolean 正確解析（`"true"` 字串 → `True`）

### Session State 統一持久化
- `sessionStorage` 存全部 session 狀態（token、kanban、mode、agents、topic、面板開關）
- 統一 saveSessionState() / restoreSessionState()

### In-Session Auto/Manual 切換
- Chat header 加 A/M 按鈕 + 輪數 + turn hint
- 後端 `set_auto` WS event 處理

### 成員面板改版
- 兩行佈局（名字上面、Chat/Think 下面）
- 面板加寬 260px

## Architecture Planning

### Persistent CLI Session（下一 phase）
- 用 pexpect 保持 CLI session 活著
- 預估：Claude 6s→2s, Gemini 27s→3s
- 需要 build_prompt 大改版（init once + delta per turn）
- Plan: `docs/plans/persistent-cli-session.md`

## Retro 教訓

- 連續 4 次 commit 未驗證的 model test 功能
- QA 說 pass 但沒從 UI 點按鈕
- sessionStorage 無法從 headless browser 驗證
- 規則：交付前必須實際跑一次（「Did I click it?」）

## Test Coverage

421 tests, all passing

## Tags

- v0.11.1 — bug fix + UI release
