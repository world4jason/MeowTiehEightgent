services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: meowtieh_test
    ports:
      - "5432:5432"
    volumes:
      - e2e_pgdata:/var/lib/postgresql/data

volumes:
  e2e_pgdata:
